#include <bits/stdc++.h>
using namespace std;
const int N = 55;
int n, m;
int main() {
	scanf("%d%d", &n, &m);
	for (int i = 1; i <= n; ++i)	scanf("%s", s1[i] + 1);
	for (int i = 1; i <= n; ++i)	scanf("%s", s2[i] + 1);
	
	return 0;
}
